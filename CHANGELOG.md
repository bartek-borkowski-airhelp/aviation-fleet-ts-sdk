## 2026-04-30 — main

Source: `bartek-borkowski-airhelp/aviation-fleet` @ [`5878527`](https://github.com/bartek-borkowski-airhelp/aviation-fleet/commit/5878527aa8ed7f79838c5862395de193ab409565)

# Changelog

### Added
- Added new `body_type` option `biplane` to the `Aircraft` type.
  
### Changed
- Updated `engine_type` in the `Aircraft` type to include `propeller` as a valid option.
- Updated `body_type` in the `Aircraft` type to include `biplane` as a valid option.
- Updated `engine_type` in the `AircraftDetails` type to include `propeller` as a valid option.
- Updated `body_type` in the `AircraftDetails` type to include `biplane` as a valid option.

---

# Changelog

All notable changes to the generated TypeScript client are documented here.
Entries are prepended automatically by the `Generate TypeScript Client` workflow.

---
